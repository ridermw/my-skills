[package]
name = "room-reader"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
cap-std = "4"
cap-fs-ext = "4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
